# Race Condition Web Tester

url : https://6c6f1dbd9a04.ngrok-free.app/

## 🚀 How to Use

1. Paste your cURL command into the designated textarea.
2. Set the number of requests you wish to send (range: 1–1000).
3. Click "Run Attack" to initiate the concurrent execution.
4. Monitor the results and perform your analysis using the results table.

### Example cURL Command:
```bash
curl -X GET 'https://api.example.com/endpoint' \
  -H 'Authorization: Bearer token' \
  -H 'Content-Type: application/json'
```
